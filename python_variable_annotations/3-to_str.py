#!/usr/bin/env python3
"""to_str function with type annotations."""


def to_str(n: float) -> str:
    """
    Returns the string representation of a float.

    Args:
        n (float): The float to be converted to a string.

    Returns:
        str: The string representation of the float.
    """
    return str(n)
